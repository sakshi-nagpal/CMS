web:
  extends:
    file: common.yml
    service: web
  environment:
   NODE_ENV: test
  links:
    - db
  command:  bash -c 'nginx && npm install && bower install --config.interactive=false --allow-root && grunt build && clusterjs server.js'
db:
  extends:
    file: common.yml
    service: db